# SPDX-License-Identifier: Apache-2.0
[package]
name = "origin-modeldiscovery"
description = "Runtime model discovery: parse provider model listings, merge with builtin catalog, and cache with a TTL"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
