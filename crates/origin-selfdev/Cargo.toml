# SPDX-License-Identifier: Apache-2.0
[package]
name = "origin-selfdev"
description = "Binary self-development: a safe-by-construction supervised hot-reload state machine with injected side effects, build queue, and reload-context resume"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"

[dev-dependencies]
tempfile = "3"
