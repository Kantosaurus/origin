[package]
name = "origin-provider-anthropic"
description = "Anthropic Messages API provider for origin"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
origin-core = { path = "../origin-core" }
origin-planner = { path = "../origin-planner" }
origin-provider = { path = "../origin-provider" }
origin-stream = { path = "../origin-stream" }
origin-cas = { path = "../origin-cas" }
origin-cassette = { path = "../origin-cassette" }
origin-multimodal = { path = "../origin-multimodal" }
async-trait = "0.1"
eventsource-stream = "0.2"
futures-util = "0.3"
pin-utils = "0.1"
dirs = "5"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls", "rustls-tls-native-roots", "stream"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
tokio = { version = "1", default-features = false }
uuid = { version = "1", features = ["v4", "v5"] }
tokio-util = { version = "0.7", features = ["io"] }
tracing = "0.1"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
wiremock = "0.6"
tempfile = "3"
