# SPDX-License-Identifier: Apache-2.0
[package]
name = "origin-scout"
description = "Read-only dependency-source research: shallow-clone planning and repo overview extraction for origin."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "1"

[dev-dependencies]
serde_json = "1"
