# SPDX-License-Identifier: Apache-2.0
[package]
name = "origin-router"
description = "Model routing strategies (architect/editor split, phase-aware, scored, quota fallback) over fed-in health/latency"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
serde_json = "1"
