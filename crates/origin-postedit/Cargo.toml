# SPDX-License-Identifier: Apache-2.0
[package]
name = "origin-postedit"
description = "Post-edit lint/test/format policy with a builtin formatter table; decision logic only, execution is the caller's job"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
serde_json = "1"
