# SPDX-License-Identifier: Apache-2.0
[package]
name = "origin-vcs"
description = "Agent-native git safety layer: shadow-git checkpoints, restore, rewind, and a lane/draft-patch model"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
serde_json = "1"
