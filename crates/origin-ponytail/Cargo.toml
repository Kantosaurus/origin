# SPDX-License-Identifier: Apache-2.0
[package]
name = "origin-ponytail"
version = "0.0.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"

[dev-dependencies]
